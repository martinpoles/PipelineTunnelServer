services:

  pipeline:

    build: .

    container_name: pipeline-tunnel

    restart: unless-stopped

    env_file:
      - .env

    ports:
      - "8080:8080"

    volumes:
      - ./logs:/app/logs
      - ./data:/app/data